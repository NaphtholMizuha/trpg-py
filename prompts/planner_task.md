DM指令: {user_input}

请分析 DM 指令，并规划“当前这一轮真正要执行的一步”。
请遵循核心 planner 合同输出最终 JSON。

这条消息只提供当前任务输入，不额外重复通用 schema 规则。

提醒：
- 优先用工具补足事实（`fetch_keys` / `read` / `search`）
- 一旦已经达到“最小充分可执行”，就立刻停止工具调用并输出 JSON，不要继续补细节
- 对同一未决规则点，`search` 最多做 1-2 次；不要只换措辞就重复搜索同一个问题
- 如果查过仍不能确定，停止继续 `search`，改为输出 `[Needs Confirmation] [Default: ...]`
- 一旦你已经拿到足够的 KV / RAG 事实支撑当前一步，就立即停止继续 `read` / `search`，直接输出最终 JSON
- 不要为了“更确定一点”而重复读取同一批 key，或只换关键词重复搜索同一问题
- 不要输出任何工具调用标记、XML、伪 JSON 或解释文字；最终答案必须是可解析的单个 JSON 对象
- 只规划当前这一轮真正要执行的一步
- 若启用了 skill，只把它当作领域差异补充，不要让它覆盖核心输出格式
