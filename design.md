# 节点梗概

- 意图分析：用LLM将DM的描述转述为相对结构化的任务，并将其入队
- 规则获取：当队列非空时从队列中取出任务，用LLM构造RAG prompt，调用查询tool
- 逻辑判定：用LLM根据任务描述和规则上下文，调用fetch_schema tool查询世界状态长什么样子，构造出表达式，并调用eval tool运行表达式,返回表达式及其结果
    - 例如判定张三使用长剑攻击哥布林：同时编译任务前置判定((zhangsan.x - goblin.x) ** 2 + (zhangsan.y - goblin.y) ** 2 < 长剑攻击范围 ** 2 )，和结果判定
    - 返回生成的表达式列表
- 状态更新：用LLM解析表达式结果，使用patch tool更新世界状态(包括logs)
- 连锁检测：查看状态更新列表，用LLM思考会带来什么新的影响，并建模为结构化任务入队，但是这里的入队需要DM人工审批
- 队列：如果空了就结束。