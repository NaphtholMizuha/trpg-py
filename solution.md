你的直觉非常敏锐！在如此复杂的检索管道（Pipeline）中，**如果重排（Rerank）时依然只用原始查询，确实会浪费掉前面“增强”所带来的语义红利，甚至导致重排效果大幅下降。**

但在决定用什么作为重排依据之前，我们需要先明确主流 Reranker 模型（如你用的 `bge-reranker-v2-m3`）的工作原理：它们大多数是基于 **Cross-Encoder（交叉编码器）** 的，且通常针对 **“短查询 -> 长文档”**（非对称检索）进行了微调，并且对**词汇重合度**和**确切语义**非常敏感。

在这个架构下，我们有三个候选者：**原始查询**、**增强查询**、**HyDE 生成的虚拟文档**。以下是深度分析及最佳实践：

---

### ❌ 为什么不建议用 HyDE 生成的文档？
虽然 HyDE 在**向量召回**阶段非常强大，但它**不适合**作为 Rerank 的输入（Query端）。
1. **长度限制：**的大多数 Rerank 模型（如 BGE-reranker）输入 token 限制在 512 左右（包含 Query + Document）。HyDE 生成的文档通常很长，如果把它作为 Query，可能会把真正需要评估的真实文档截断。
2. **非对称性被破坏：** Reranker 是为了判断“一句话”和“一篇文章”的相关性而训练的。如果你输入“一篇文章（HyDE）”和“一篇文章（真实文档）”，模型的打分逻辑可能会产生偏差。
3. **幻觉污染：** HyDE 包含 LLM 猜测的细节（幻觉）。如果真实文档没有这些细节，Reranker 可能会因为“细节不匹配”而给真实文档打低分。

### ⚠️ 为什么原始查询（Original Query）不够好？
回到你上一个例子：“野蛮人生命骰”。
如果你用这个原始查询去重排一段纯英文的 Markdown（包含 `Barbarian`, `Hit Die`, `1d12`），即使这段英文被 HyDE 成功召回了，Reranker 依然可能因为**中英跨语言差异**和**术语不完全对应**，给出一个极低的分数（比如 0.23），从而把它排在后面。

### ✅ 最佳选择：使用“净化后的增强查询（Enhanced Query）”
在你的 Pipeline 中，“增强查询”通常包括了**意图补充、同义词扩展、特别是多语言翻译**。这是 Reranker 最需要的“弹药”。

**最佳的 Rerank Query 应该构造为：**
`[原始查询] + [核心翻译/扩展词]`

以“野蛮人生命骰”为例，Rerank 时的输入应该是：
> `"野蛮人生命骰 (Barbarian Hit Dice, Hit Points, Class Features)"`

**为什么这样最好？**
1. **保留核心意图：** 包含了原始的中文，确保没有偏离用户本意。
2. **填补词汇鸿沟（Vocabulary Gap）：** 补充的英文术语（Barbarian Hit Dice）能让 Cross-Encoder 瞬间在英文文档 `classes.md` 中抓取到极高的注意力权重（Attention）。
3. **长度适中：** 不会像 HyDE 那样占用几百个 Token，把宝贵的上下文窗口留给被评估的文档。

---

### 🚀 你的 Pipeline 最佳数据流向建议

为了让这个系统达到顶级效果，你的架构数据流应该这样设计：

1. **用户输入:** `"野蛮人生命骰"` ($Q_{orig}$)
2. **LLM 增强 (Query Rewrite):**
   * *提取核心词与翻译:* `"Barbarian Hit Dice, Hit Points"` ($Q_{keywords}$)
   * *生成 HyDE 虚拟文档:* `"A barbarian uses a d12 for their hit die. At first level..."` ($D_{hyde}$)
3. **召回阶段 (RRF 混合搜索):**
   * **向量检索 (Dense):** 使用 $D_{hyde}$ 去做相似度匹配（因为向量模型最喜欢长文本比长文本）。
   * **关键词检索 (BM25):** 使用 $Q_{orig} + Q_{keywords}$ 去做精确匹配。
   * **RRF:** 将两路召回结果混合，得出 Top 20 候选文档。
4. **重排阶段 (Reranking):**
   * **Rerank Query 设置为:** `$Q_{orig} + " | " + $Q_{keywords}$`
   * *实际输入 Reranker 的结构:* `[CLS] 野蛮人生命骰 | Barbarian Hit Dice [SEP] (真实的文档块内容) [SEP]`

### 💡 总结
**重排序的依据，应该是包含了“领域术语翻译”和“同义词扩展”的【精简版增强查询】。**
绝对不要用长篇大论的 HyDE 文档去重排，也尽量避免在跨语言/垂直领域中使用裸的原始查询。这样你既能享受 HyDE 带来的高召回率，又能利用增强查询的精准词汇让 Reranker 准确发力。