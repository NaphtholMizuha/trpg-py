## 1. 治疗封顶能力

- [x] 1.1 为 `heal.apply` 设计并实现目标生命上限解析，支持显式路径、路径模板和默认布局兜底。
- [x] 1.2 更新治疗写回逻辑，确保生命值不会超过解析到的目标上限。

## 2. 回归测试扩展

- [x] 2.1 扩充检定测试，覆盖攻击天然 1 触发 `crit_fail`、优势取高、劣势取低，以及优势与劣势抵消。
- [x] 2.2 扩充操作测试，覆盖范围查询目标过滤、资源不足时状态保持不变，以及治疗不能超过上限。
- [x] 2.3 为 `main.py` 增加集成测试，验证示例输出会展示任务状态、步骤摘要和关键状态变更。
- [x] 2.4 如有必要，补充或调整测试夹具与示例任务，使边界场景可稳定复现。

## 3. 验证

- [x] 3.1 运行 `python -B -m unittest discover -s tests -v` 并确认新增与既有测试全部通过。
