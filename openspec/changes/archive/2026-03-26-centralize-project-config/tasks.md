## 1. 统一配置骨架

- [x] 1.1 新增统一配置模块（如 `trpg_py.config`），定义集中配置入口与类型化配置模型
- [x] 1.2 在项目根目录新增唯一运行配置文件（如 `config/config.toml`）及最小示例内容
- [x] 1.3 实现统一配置加载、TOML 解析与校验错误输出
- [x] 1.4 实现共享配置访问方式，确保业务模块消费同一份配置对象而不是各自重复解析

## 2. 迁移 planner 配置

- [x] 2.1 将 `trpg_py.agent.planner` 中的模块级 `DEFAULT_*` 与 `ENV_*` 运行配置迁移到统一配置模型
- [x] 2.2 修改 planner factory，使其默认从统一配置入口读取模型、接入点、超时、重试、规划轮数和工具预算
- [x] 2.3 保留测试/显式调用所需的配置注入能力，同时避免业务路径绕过统一配置入口

## 3. 迁移 search 配置

- [x] 3.1 将 `trpg_py.agent.tools.search` 中的 API、Qdrant 和检索默认配置迁移到统一配置模型
- [x] 3.2 修改默认搜索器与相关构造路径，使其从统一配置入口读取默认参数
- [x] 3.3 删除或收缩 search 模块内部对独立环境变量和隐式默认值的长期依赖

## 4. 验证与文档

- [x] 4.1 新增统一配置加载测试，覆盖成功加载、文件缺失和字段非法场景
- [x] 4.2 调整 planner 和 search 测试，验证它们默认消费统一项目配置
- [x] 4.3 更新 README 和运行说明，明确项目运行配置仅以单一 TOML 文件为准
