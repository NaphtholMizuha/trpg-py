## 1. 规范与边界对齐

- [x] 1.1 审查 `project-config`、`demo-runner` 与 `fetch-keys-tool` 的现有实现和文档，列出哪些配置读取点属于 `trpg_py` 包内，哪些仅属于仓库脚本。
- [x] 1.2 更新实现说明与贡献约定，明确统一配置的强制治理范围仅覆盖 `trpg_py` 包内长期运行配置。

## 2. 包内配置治理收口

- [x] 2.1 保留并检查 `trpg_py` 包内模块对统一配置入口的依赖，确保新增包内默认配置仍通过统一配置模型扩展。
- [x] 2.2 清理或调整把仓库根目录脚本默认值误当作统一配置真相的代码路径，避免规范与实现边界继续混淆。

## 3. 仓库脚本约束放宽

- [x] 3.1 调整 `main.py` 相关实现或说明，使 demo 脚本在没有统一配置的前提下仍可通过局部默认值或 CLI 参数运行。
- [x] 3.2 调整 `test_fetch_keys.py` 相关实现或说明，使演示脚本在没有统一配置的前提下仍可展示核心行为。

## 4. 验证

- [x] 4.1 补充或更新测试，验证包内模块继续使用统一配置，而仓库脚本不再被统一配置强制绑定。
- [x] 4.2 更新 README 或开发文档，说明哪些配置必须进入统一配置模型，哪些脚本级默认值可以继续局部维护。
