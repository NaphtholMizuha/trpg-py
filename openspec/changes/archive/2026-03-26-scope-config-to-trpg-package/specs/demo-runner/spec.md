## 新增需求

### 需求:Demo 仓库脚本入口不必须接入统一项目配置
系统必须允许 `main.py` 这类仓库脚本入口继续以脚本局部默认值、注册表默认行为或 CLI 参数运行 demo，禁止把统一项目配置当作该脚本入口的强制前置条件。

#### 场景:开发者直接运行 demo 脚本
- **当** 开发者执行 `python -B main.py fireball` 或其他 demo 命令
- **那么** 系统可以在不依赖统一项目配置的前提下运行对应 demo
- **那么** 调用方仍然可以通过显式参数或脚本内约定得到稳定结果

## 修改需求

## 移除需求
