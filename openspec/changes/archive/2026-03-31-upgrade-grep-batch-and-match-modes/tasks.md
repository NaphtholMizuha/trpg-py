## 1. grep 协议重构

- [x] 1.1 重构 `grep` 输入 schema，使其支持一次调用提交多组关键词请求，并为每组请求表达可嵌套的 `and` / `or` 布尔条件。
- [x] 1.2 重构 `grep` 输出 schema，使每条返回项包含命中的扁平化状态行文本及匹配解释，并保持 `ok` / `no_match` / `error` 结构化语义。

## 2. 匹配逻辑实现

- [x] 2.1 为当前 state 增加扁平化遍历逻辑，生成形如 `a.b.c = data` 的文本行集合作为 `grep` 的匹配对象。
- [x] 2.2 实现批量请求执行与可嵌套 `and` / `or` 表达式求值逻辑，并返回所有命中的扁平化行而不是旧的路径候选 top-N 语义。
- [x] 2.3 更新 runtime guard 的主题归一化或缓存键逻辑，使批量输入不会错误复用旧的单请求缓存结果。

## 3. 日志与验证

- [x] 3.1 更新 `grep` 的 loguru 输入输出日志，记录批量请求摘要、匹配模式和命中行数量。
- [x] 3.2 新增或更新 `grep` 单元测试，覆盖批量输入、`and` / `or` 组合表达式、全部命中行返回和无命中场景。
- [x] 3.3 更新 `src/smoke/test_grep.py`，展示新协议下的批量输入与布尔组合输出边界。
