## 1. 根目录 API 收口

- [x] 1.1 盘点 `trpg_py` 包根目录当前薄 facade 模块及其对应迁移目标路径
- [x] 1.2 确认 `trpg_py.__init__` 保留的稳定公共 API 与 `trpg_py.errors` 的顶层保留策略
- [x] 1.3 删除根目录薄 facade 文件并更新包结构说明，确保根目录只剩稳定入口与真实顶层模块

## 2. 导入路径迁移

- [x] 2.1 将项目内实现代码的相关导入改为使用 `trpg_py.engine...`、`trpg_py.store...` 或 `trpg_py` 顶层稳定 API
- [x] 2.2 更新测试、示例和脚本中的旧根目录模块导入，改为新导入路径
- [x] 2.3 补充或调整测试，覆盖 `trpg_py.__init__` 顶层导出与子包导入路径

## 3. 验证与发布准备

- [x] 3.1 运行回归测试，确认删除 facade 后项目内不再依赖旧根目录模块
- [x] 3.2 更新文档或发布说明，列出被移除的模块及对应迁移路径
