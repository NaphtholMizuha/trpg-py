## 1. 模块重组

- [x] 1.1 创建 `trpg_py/engine/` 子模块及其基础导出结构
- [x] 1.2 将执行器、原子操作、状态访问和骰子相关实现迁移到 `engine` 下的职责化模块
- [x] 1.3 调整包内导入关系，确保执行链路在新结构下正常工作

## 2. 兼容入口

- [x] 2.1 保留 `trpg_py` 顶层稳定公共 API 的兼容导出
- [x] 2.2 为 `trpg_py.engine` 提供显式可导入的引擎入口
- [x] 2.3 检查 `main.py` 与示例运行路径，确保现有 demo 使用方式不因重组而改变

## 3. 验证

- [x] 3.1 更新或新增测试，覆盖 `trpg_py` 顶层导入与 `trpg_py.engine` 导入
- [x] 3.2 运行相关单元测试，确认执行语义未因模块迁移发生回归
- [x] 3.3 运行至少一个 demo 场景，确认主流程仍可正常执行
