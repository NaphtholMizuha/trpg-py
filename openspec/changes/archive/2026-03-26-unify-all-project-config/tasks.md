## 1. 扩展统一配置模型

- [x] 1.1 扩展 `trpg_py.config` 与 `config/config.toml`，加入 demo runner 与脚本默认行为所需的配置分区
- [x] 1.2 明确并实现“项目级默认行为 vs 静态 fixture / 领域常量”的配置边界说明与辅助结构
- [x] 1.3 为后续新增项目级配置补统一接入约束，避免新增模块继续引入独立 `DEFAULT_*` 或独立配置文件

## 2. 迁移 demo runner 默认配置

- [x] 2.1 盘点 `main.py` 中属于项目级默认行为的参数，并迁移到统一配置入口
- [x] 2.2 保留 CLI 显式覆写能力，同时确保未覆写时默认行为来自统一项目配置
- [x] 2.3 更新 demo runner 相关说明与示例，使开发者知道如何通过统一 TOML 调整默认行为

## 3. 迁移 fetch_keys 演示脚本默认配置

- [x] 3.1 盘点 `test_fetch_keys.py` 中属于长期默认行为的输入来源与默认参数，并迁移到统一配置入口
- [x] 3.2 保留脚本显式传参能力，同时确保未传参时脚本默认行为来自统一项目配置
- [x] 3.3 如有必要，将脚本默认输入改为通过统一配置引用外部 fixture，而不是继续写死在脚本常量中

## 4. 验证与治理

- [x] 4.1 新增统一配置覆盖 demo runner 与 `fetch_keys` 演示脚本默认行为的测试
- [x] 4.2 补充 README 或运行说明，明确项目级新增配置必须先扩展统一配置模型
- [x] 4.3 检查仓库剩余配置入口，确认本次范围内的脚本级长期默认配置已完成收敛
