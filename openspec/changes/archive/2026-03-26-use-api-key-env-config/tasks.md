## 1. 配置模型迁移

- [x] 1.1 将 `trpg_py.config` 中 planner 与 search 的统一配置模型从 `api_key` 迁移为 `api_key_env`。
- [x] 1.2 在配置加载阶段解析 `api_key_env` 指向的环境变量，并在缺失或空值时抛出明确配置错误。
- [x] 1.3 更新 `config/config.toml` 示例，使其只保存环境变量名而不保存密钥值。

## 2. 配置消费方迁移

- [x] 2.1 调整 planner 默认配置解析与相关工厂逻辑，确保未显式传参时使用统一配置解析后的密钥。
- [x] 2.2 调整 search 默认配置解析与相关默认构造逻辑，确保未显式传参时使用统一配置解析后的密钥。
- [x] 2.3 保持显式传入 `api_key` 参数时的优先级与现有行为兼容。

## 3. 文档与验证

- [x] 3.1 更新 README、示例命令和说明文档，明确开发者需要设置环境变量而不是把密钥写入配置文件。
- [x] 3.2 更新 `tests/config_helpers.py`、项目配置测试以及 planner/search 相关断言，使测试样例改用 `api_key_env`。
- [x] 3.3 运行相关测试，验证配置迁移未破坏默认构造、显式覆写和错误提示行为。
