## 1. 执行框架骨架

- [x] 1.1 建立 DnD5e 判定解析器的模块结构，包含任务模型、执行器、状态路径工具、操作注册表和掷骰接口
- [x] 1.2 定义任务文档的顶层结构，覆盖 `task_id`、`version`、`policy`、`context` 和 `steps`
- [x] 1.3 定义统一步骤结构与值引用模型，覆盖 `id`、`type`、`kind`、`tags`、`when`、`args` 与 `$ref`
- [x] 1.4 实现点分路径读取/写入工具，覆盖字典键、列表索引、缺失字典节点创建和非法索引报错
- [x] 1.5 定义目标集合与基础空间数据结构，约定 actor 的坐标、阵营、标签和存活状态字段
- [x] 1.6 定义统一检定步骤与显式骰子规格的数据结构，覆盖 `type=check`、`kind`、`tags`、`dice`、`modifier` 和四态 `outcome`

## 2. 执行器与数据流

- [x] 2.1 实现值解析器，支持 `literal`、`state`、`context` 和 `result` 四类输入来源
- [x] 2.2 实现顺序步骤执行器，支持步骤校验、按顺序运行、`when` 条件跳过和运行时失败即停止
- [x] 2.3 实现统一的状态提交与执行报告汇总，记录步骤状态、输出、事件、错误和已提交变更
- [x] 2.4 为执行器注入可替换的掷骰接口，支持测试中的固定结果与可复现随机序列
- [x] 2.5 让执行器支持目标集合作为步骤输出与后续输入，覆盖单体与多目标数据流
- [x] 2.6 在任务校验与参数解析阶段校验顶层任务结构、统一步骤字段与 `$ref` 引用语法
- [x] 2.7 在任务校验与参数解析阶段校验 `type=check`、`kind`、`tags` 与显式骰子规格，拒绝把优势/劣势和全部语义塞进单个不可分解公式的输入

## 3. DnD5e 原子操作

- [x] 3.1 实现有限的步骤类型分派机制，覆盖 `select`、`check`、`damage`、`heal`、`resource`、`effect` 和 `state`
- [x] 3.2 实现选择类操作，至少覆盖显式单体选择、范围查找和基础过滤输出 `target_ids`
- [x] 3.3 实现统一的检定类操作入口，使用 `type=check` 与 `kind` 区分攻击、豁免、能力和技能检定
- [x] 3.4 为检定类操作实现 `tags` 语义，至少支持 `nat`、`adv`、`disadv`
- [x] 3.5 为检定类操作实现四态 `outcome`，并按检定类型区分天然 20/1 的默认语义
- [x] 3.6 实现伤害与治疗操作，支持显式数值、骰子结果引用以及按配置乘数结算最终变化量
- [x] 3.7 实现伤害骰与暴击规则分离的结算逻辑，避免通过单个公式字符串隐式编码暴击
- [x] 3.8 实现资源消耗操作，确保资源不足时失败且不提交扣减
- [x] 3.9 实现效果添加与移除操作，支持写入来源、持续时间和可被后续步骤读取的效果记录
- [x] 3.10 将核心原子操作接入操作注册表，并补充可表达“显式掷骰/范围选目标/豁免/伤害/状态写回”流程的示例任务 JSON

## 4. 测试与验证

- [x] 4.1 为任务校验、路径解析、条件跳过和运行时失败语义编写单元测试
- [x] 4.2 为顶层任务结构、统一步骤字段和 `$ref` 数据流解析编写执行器测试
- [x] 4.3 为 `type=check`/`kind`/`tags` 校验、四态 `outcome`、优势/劣势归并和天然 20/1 语义编写检定操作测试
- [x] 4.4 为目标选择、半伤害、资源不足和效果增减编写原子操作测试
- [x] 4.5 编写至少一个端到端战斗场景测试，验证任务 JSON 能驱动状态从输入到输出的完整变化
- [x] 4.6 更新 README 或示例文档，说明状态结构约定、任务 JSON 写法和最小可运行示例

## 5. 扩展性增强

- [x] 5.1 为选择类操作设计并实现 `field_map`，支持把 `id`、`side`、`alive`、`tags`、`position.x`、`position.y` 映射到任意状态字段
- [x] 5.2 为检定类操作实现显式依赖路径读取，至少覆盖 `target_ac_path`、`target_ac_path_template`、`modifier_path` 和 `modifier_path_template`
- [x] 5.3 为伤害、治疗和效果操作实现显式写回路径模板，至少覆盖 `target_hp_path_template` 与 `effects_path_template`
- [x] 5.4 补充一个使用非默认状态结构的完整示例任务 JSON，验证显式字段映射确实可行
- [x] 5.5 为 `field_map` 与显式路径模板编写测试，覆盖自定义坐标字段、自定义 AC 字段和自定义效果列表字段
