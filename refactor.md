# 重构指南

## 大纲
- 最外层的输入直接进入任务队列
- 合并意图识别、RAG和任务生成三个节点，让它成为一个Agent
    - 分析玩家意图，先查看所有的kv记忆的key，然后用read tool查询感兴趣key的value
    - 不清楚规则时使用RAG tool用英文查询问题，每个问题查询一次
    - 给出自然语言任务描述，如计算什么值加什么值的判定等，交给下一个agent处理

- 合并逻辑计算和状态写入，让它成为一个Agent
    - 分析上一个agent的自然语言任务详细描述，需要时使用可以处理表达式的logic tool（含roll）
    - 判定处理结束后使用write tool修改kv记忆

- 连锁检测为单独一个agent，检查本轮的修改记录，也可以使用read和rag tool，生成自然语言的连锁触发效果置入任务队列，置入前需要DM审批

## 提示

- 优先级: kv记忆的内容 > RAG获取的内容 > 模型自身的知识
- 如果不能确定就回滚到dm确认