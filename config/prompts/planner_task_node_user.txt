{
    "instruction": "{{instruction}}",
    "requirements": [
      "Normalize the instruction.",
      "Write a TaskDraft as an honest execution brief, not as a category label.",
      "Start from the intended world change or question, not from mandatory task prototype classification.",
      "Identify the execution goal, affected entities, critical prerequisites, and likely writes before you finalize path bindings.",
      "Draft provisional judgments before finalizing reads or writes.",
      "Use tools only to close concrete state gaps or rule gaps.",
      "Use available tools when they help gather state or rule evidence for the task draft.",
      "Use grep as the default tool for state discovery and treat its matches as structured evidence with key, value, and sim.",
      "When using grep, prefer focused expressions that combine an entity and a fact instead of broad single-word queries.",
      "When judgments involve multiple entities, grep each relevant entity separately before finalizing reads or writes.",
      "Do not use a small grep limit by default. Prefer the full ranked result set unless truncation is clearly necessary.",
      "If search is needed, preserve explicit rule terms such as spell names, action names, condition names, or reaction names as anchors whenever possible.",
      "Do not rewrite a named rule into a purely semantic query when the rule identity matters.",
      "Use more semantic search only when there is no reliable rule-term anchor.",
      "When a rule-first search result tells you which state facts matter, use that knowledge to drive the next grep queries.",
      "Write a task draft as an execution plan, not an evidence checklist.",
      "Write judgments before binding reads or writes.",
      "reads, writes, missing_info, and assumptions must be derived from the judgments instead of being guessed independently.",
      "evidence must contain only short excerpts for the next planner stage, not a tool log or a full rule quotation.",
      "Use evidence for summarized rule clues or decisive state facts that help the next stage understand the task shape.",
      "reads must contain only exact dot paths supported by the instruction or tool evidence.",
      "writes must contain only exact dot paths supported by the instruction or tool evidence.",
      "states must contain only short state evidence lines that directly support the final reads, writes, or judgments.",
      "Do not put prose descriptions inside reads or writes.",
      "Do not invent paths like monsters.*, weapons.*, or nested fields that are not confirmed by evidence.",
      "judgments must describe actual game resolution logic, not research steps like confirm/check/inspect.",
      "If a judgment says a target makes a saving throw, reads should include the target's exact save path when evidence confirms it.",
      "If a target HP path is confirmed and the instruction is an attack or damage action, writes should usually include that HP path.",
      "If an actor resource path is confirmed and the instruction consumes it, writes should usually include that resource path.",
      "If a named spell is confirmed, bind the spell's native level or explicit upcast level before choosing a spell-slot path.",
      "Do not bind an arbitrary spell-slot level just because it appeared first in grep.",
      "If an area effect depends on burst center, line coverage, radius, cone, or other coverage relation, include that coverage judgment before resolving each affected creature.",
      "If an area effect depends on position or distance, reads and states should include the exact position evidence you actually used.",
      "If position, burst-center, coverage, or default spell-level evidence cannot be confirmed safely, add that gap to missing_info instead of collapsing the task into a complete-looking single-target plan.",
      "When grep returns multiple matches, favor higher-sim items and keep only the relevant supporting evidence in states.",
      "Keep states short and only include state evidence lines that actually support the final draft.",
      "Treat any few-shot examples as demonstrations of evidence gathering and gap exposure, not as category templates to imitate mechanically.",
      "The few-shot set intentionally excludes `火球术 Fireball`; do not assume a hidden Fireball template."
    ]
}
