Intent: {{intent}}
Goal: {{goal}}
Requests:
{{requests_block}}

Existing ask responses:
{{ask_responses_json}}

Current state evidence:
{{state_evidence_json}}

Current rule evidence:
{{rule_evidence_json}}

Current tool trace:
{{trace_block}}

要求：
- 不要把 requests 写成字段名列表，例如 `caster_id` 或 `target_id`。
- 不要把 requests 写成问句列表，例如 “施法者是谁？”。
- 你必须自己判断当前证据是否已经足够安全 grounding；是否发起 ask 必须由你的推理决定，而不是机械地因为看到 actor、target、position 等缺口就立刻提问。
- 如果现有证据仍允许你继续收集规则或状态证据，优先继续取证；只有在缺口已经阻止你安全落地时，才发起 ask。
- 当你决定 ask 时，直接调用 `ask` 工具，并提供结构化的 `question_id`、`prompt`、`options`、默认值和自定义输入入口。
- 当你已经有足够证据时，返回最终结构化结果，包含 `status`、`action`、`resolved_entities`、`derived_context`、`notes`。
- `status` 只能是 `ready` 或 `blocked`。如果需要人工澄清，不要直接输出 `needs_human`，而是调用 `ask` 工具。
- `notes` 只保留高价值判断、缺口或阻塞原因，不要复述全部证据。
