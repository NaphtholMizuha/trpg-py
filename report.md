# TRPG-PY 项目介绍

这个项目叫 `trpg-py`，它本质上是一个面向 TRPG 场景的多智能体规则执行系统。我的理解是，它想解决的不是普通聊天型 AI 的问题，而是让模型能够在桌面角色扮演游戏里，比较稳定地完成“理解玩家指令、查规则、执行动作、处理连锁反应、更新世界状态”这一整套流程。

如果用一句比较适合面试的方式来概括，我会说：这是一个把大模型接入 TRPG 规则结算链路的工程化项目，重点不在生成文案，而在结构化执行和可验证性。

## 1. 项目背景

TRPG 场景和普通对话系统不太一样。玩家的一句输入，比如“马利克对艾尔德拉施放魔法飞弹”，背后其实对应的是一连串需要精确处理的逻辑：

- 要识别行动者和目标是谁
- 要判断这是不是法术动作，是否消耗法术位
- 要根据规则决定会不会触发反应
- 如果有人打断，比如护盾术、法术反制，要处理优先级和覆盖关系
- 最后还要把角色生命值、法术位这类状态准确写回

如果这些步骤都交给一个大模型一次性输出，结果往往不稳定，也很难排查错误。所以这个项目采用的思路是，把复杂过程拆成多个职责明确的 agent，再用工作流把它们串起来。

## 2. 核心设计

这个项目最核心的部分，是 `planner`、`executor` 和 `resolver` 三个 agent。

`planner` 负责把自然语言输入转成结构化任务。它不直接结算结果，而是先明确这一步“谁对谁做了什么”，以及可能影响哪些字段。比如它会抽取 `actor`、`target`、`write_targets`，并结合上下文把任务标准化。

`executor` 负责执行单步任务。它会根据任务内容决定资源消耗、主要效果、条件效果和叙述文本。例如是否扣法术位、是否造成伤害、是否需要掷骰。这里它还限制了工具使用范围，只允许调用数值和掷骰相关的 `evaluate` 工具，尽量避免 agent 产生越权行为。

`resolver` 负责处理同一个结算窗口里的多个执行结果。当多个动作同时影响同一路径时，它需要判断哪些结果保留，哪些结果被覆盖或者丢弃。这个设计比较贴近 TRPG 里的真实流程，因为很多动作并不是线性结算的，而是存在反应链和打断链。

整个系统通过 `LangGraph` 串成了一个显式工作流，大致流程是：

`planner -> task_approval -> executor -> window_review -> resolver -> commiter`

这里我觉得比较有亮点的是“结算窗口”这个概念。项目不是每执行一个动作就立刻写回状态，而是先把同一轮相关动作收集到一个 window 里，等窗口关闭后再统一结算。这样就能更自然地处理像“魔法飞弹 -> 护盾术 -> 法术反制”这种多步交互。

## 3. 工程实现

项目底层有一套比较清晰的工具和状态设计。

首先，世界状态不是随便拼接的文本，而是通过 KV 形式维护，再进一步细化到字段级路径，比如：

- `Aldera.combat.HP`
- `Malik.spell_slots.1环`

这样做的好处是，模型输出可以被限制在字段级更新，而不是整段自然语言覆盖，状态修改更可控，也更方便评测。

其次，项目内置了几类核心工具：

- `search`：做规则检索，用于查 D&D 5e SRD 相关内容
- `evaluate`：做掷骰和数值表达式计算
- `read` / `fetch_keys`：读取当前 world state
- `write_fields`：把字段级变更真正写回状态

从工程约束上看，项目其实做了不少“防模型失控”的设计。比如：

- `planner` 会规范化 actor、target 和 write target
- `executor` 只允许修改上下文里已知的字段路径
- `resolver` 只允许输出窗口里已经出现过的路径
- 最后的写回节点只接受字段级 path，而不是任意 key 覆盖

这些限制虽然会增加实现复杂度，但能明显提升系统稳定性。

## 4. 项目亮点

我觉得这个项目最值得讲的亮点有三个。

第一，它不是“单模型直接回答”，而是把一个复杂任务拆成了多阶段职责分离的 agent workflow。这种方式更符合复杂业务系统的工程化思路，也更容易调试和替换单个模块。

第二，它把 TRPG 里很关键但很难做的“反应链”和“结算窗口”建模出来了。比如我看到仓库里有一个 workflow case，就是魔法飞弹、护盾术和法术反制的连续结算。这个 case 不只是看最后有没有伤害，还会检查任务序列、执行序列、中断顺序、窗口运行次数，以及窗口是否正确关闭。说明这个项目不是停留在 demo 层面，而是在认真做复杂场景覆盖。

第三，它非常重视评测。项目里不仅有 `planner`、`executor`、`resolver` 的单模块测试，还有 workflow 级别的端到端评测。评测不是单纯对答案，而是检查结构化产物，比如字段变化、路径是否合法、状态是否符合预期。这种评测体系对于 LLM 项目来说非常重要，因为很多问题如果没有结构化评测，很难持续迭代。

## 5. 难点和挑战

如果在面试里讲这个项目，我觉得可以重点强调两个难点。

第一个难点是，TRPG 规则本身就有很多例外和打断逻辑。也就是说，系统不能只处理“单步动作”，还要处理“动作执行到一半被插入反应”的情况。这也是为什么项目要专门设计 resolution window，而不是简单串联 planner 和 executor。

第二个难点是，大模型天然容易输出看起来合理、但不完全符合约束的结果。所以项目在很多地方增加了结构化校验和 sanitize 逻辑。比如 executor 会过滤非法路径，resolver 会清理未知变更，最终 commiter 只接受字段级写回。这些约束其实是在把模型能力放进一个更可信的工程框架里。

## 6. 我会怎么总结这个项目

如果面试官让我用比较简短的话总结，我会这样说：

`trpg-py` 是一个面向 TRPG 规则执行场景的多智能体系统。它把玩家输入拆解成规划、执行、结算三个阶段，并通过 LangGraph 组织成完整工作流。项目的重点不只是让模型“会说”，而是让模型能够在结构化状态、规则检索和评测约束下稳定完成复杂动作结算，尤其是处理带有反应链和状态冲突的场景。

如果面试官继续追问，我会再补一句：这个项目比较有价值的地方，在于它体现了如何把 LLM 从“生成式能力”落到“工程化执行能力”，包括状态约束、工具边界、结构化输出和自动化评测。
